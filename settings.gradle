pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "modern"
def includeFeatureModule = { name ->
    include ":${name}"
    project(":${name}").projectDir = new File(rootDir, "modules/feature/${name}")
}

include ':app'
includeFeatureModule("list")